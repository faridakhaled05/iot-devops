# Copy to .env and fill in real values. Never commit .env.
# cp .env.example .env

# Docker Hub (or other registry) login for docker push
DOCKER_USERNAME=your-dockerhub-username
DOCKER_PASSWORD=your_token_or_app_password

# Image tag used for all three services (e.g. v1.0, latest)
IMAGE_TAG=v1.0
